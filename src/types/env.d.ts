/**
 * TypeScript declarations for environment variables loaded via react-native-dotenv.
 * Import values in code with: import { SUPABASE_URL, SUPABASE_ANON_KEY } from '@env';
 */
declare module '@env' {
  export const SUPABASE_URL: string;
  export const SUPABASE_ANON_KEY: string;
}
